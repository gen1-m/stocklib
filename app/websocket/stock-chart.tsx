import { AreaChart } from "recharts"
import type { Trade } from "@/types/globals";

export default function StockChart(params: {
  trades: Trade[],
}) {
 
  const trades = params.trades;

  return (
    <div>
    </div>
  )
}
